[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# SvelteKit handles all routing
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/render"
  status = 200
